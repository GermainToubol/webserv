LEO:
-> Changer inet_addr to getaddrinfo
x -> sort la listing page (ondiraitque c'estsort mais sur d'autres env ca ne l'est pas)
x -> faire la reception de la requete en 2 etapes 1 header 2 body apres avoir lu content lenght
x -> check le content lenght end dans le get request
x -> fix reformat file sans /
x -> 1 bind par ip:port
x -> Stat dans utils
x -> rajouter le check de la taille de l'uri
x -> need to test CGI and CGI timeout
x -> need to free tout correctement

x -> chnager le path dudefault filedirectory // faire tous les checks des file absolute or relative via le config file
x ->changer la facon de set le header dans response 

